import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

const rootDomEl = document.querySelector('#root');

const titleElement = <h1 class="title">Todo List</h1>;

ReactDOM.render(titleElement, rootDomEl);
